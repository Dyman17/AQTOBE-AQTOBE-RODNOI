{
  "name": "aqbobek-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run dev:clean-ports && concurrently -k -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:clean-ports": "node scripts/clean-ports.mjs",
    "dev:backend": "node scripts/python-runner.mjs backend-dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "setup": "npm install --no-fund --no-audit && npm run setup:frontend && npm run setup:backend",
    "setup:frontend": "npm --prefix frontend install",
    "setup:backend": "node scripts/python-runner.mjs setup-backend",
    "test:scripts": "node --test scripts/python-runner.test.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
